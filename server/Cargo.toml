[package]
name = "realm-icons"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
axum = "0.8.9"
ico = "0.5.0"
# Filters and shapes only: no text layout, no system fonts, no raster images.
resvg = { version = "0.48.1", default-features = false }
rust-embed = { version = "8.12.0", features = ["mime-guess", "include-exclude"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "net", "signal"] }
ttf-parser = "0.25.1"

# Size over speed: this binary idles on the box.
[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dev-dependencies]
tower = { version = "0.5.3", features = ["util"] }
